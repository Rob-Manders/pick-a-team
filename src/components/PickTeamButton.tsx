import React from 'react'
import styled from 'styled-components'

const Button = styled.button`
  background-color: red;
`

export default function PickTeamButton() {
  return <Button>Pick a Team!</Button>
}
